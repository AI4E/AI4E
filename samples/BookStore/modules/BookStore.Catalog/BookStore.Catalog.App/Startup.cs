namespace BookStore.Catalog.App
{
    /* The client-side part cannot have a startup currently, but we need this as anchor type in the server project. */
    public class Startup
    {
        ///* This is never called but is needed for .blazor.config files beeing created. */
        private static void Main() { }
    }
}
