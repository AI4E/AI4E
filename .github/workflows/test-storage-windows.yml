name: Test Storage Windows

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.101
    - name: Install dependencies
      run: dotnet restore src\Storage\AI4E.Storage.sln
    - name: Build
      run: dotnet build src\Storage\AI4E.Storage.sln --configuration Release --no-restore
    - name: Test
      run: dotnet test src\Storage\AI4E.Storage.sln --no-restore --verbosity normal